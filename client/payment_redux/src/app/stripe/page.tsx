import React from "react";
import StripeCheckout from "../component/stripe_chekout/StripeCheckout";

const page = () => {
  return (
    <div>
      <StripeCheckout />
    </div>
  );
};

export default page;
